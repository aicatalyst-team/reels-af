"""Deterministic planning helpers — no LLM calls.

  beats        — ScriptDraft → list[Beat] (video planning)
  cards        — WordTiming[] → list[Card] (subtitle layout)
  font_metrics — Montserrat Bold width tables used by cards
  safe_zone    — 9:16 canvas + subtitle/accent positioning constants
"""
